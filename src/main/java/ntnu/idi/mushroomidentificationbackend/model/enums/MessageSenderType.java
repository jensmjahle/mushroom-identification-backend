package ntnu.idi.mushroomidentificationbackend.model.enums;

/**
 * Enum representing the type of sender for a message.
 * This is used to distinguish between messages sent by users
 * and messages sent by administrators.
 */
public enum MessageSenderType {
  USER,
  ADMIN
}
